@model AS_CRM.PSprint

@{
    ViewBag.Title = "Details";
}

<h2>Sprint @Model.Nombre - del @Model.FechaIncio al @Model.FechaIncio</h2>

<div>
    <hr />
    <div class="container-fluid">
        <div class="row">

            @foreach (var _e in (ViewData["estados"] as IList<PEstado>))
            {
                <div class="col-md-2" style="border:5px solid white; height:40px; align-items:center; background-color:@_e.Color; border-radius:8px;">
                    <h4 style="text-align:center; height:10px;margin-top:6px;">@_e.Nombre</h4>
                </div>
            }
            @foreach (var _e in (ViewData["estados"] as IList<PEstado>))
            {


<div class="col-md-2" style="border:5px solid white; height:70vh; align-items:center; background-color:#f2eeee; overflow-y:auto; padding-top:20px;">
    @if (ViewBag.userfilter == true)
    {
        foreach (var _t in Model.PTareas.Where(w => w.Estado_Id == _e.Id).OrderBy(o => o.FechaEntrega))
        {
            <div class="card" style="border: 1px solid #dcdbdb; background-color: @_e.Color; border-radius: 5px;  height:120px;font-size:9pt; ">
                <h4 class="bold" style="padding-left:5px; border-bottom:1px solid #c7c4c4; background-color:#e8e5e5; margin-top:0px;">
                    @_t.Nombre
                    <span><a class="btn-grid" href="@Url.Action("EditModal", "PTareas", new { id = _t.Id }) "><img src="~/Content/image/iconos_Buscar.png" style="width:14px; height:14px;" /></a></span>
                    <span><a class="btn-grid" href="@Url.Action("CreateParteHoras", "PPartehoras", new { idt = _t.Id }) "><img src="~/Content/image/iconos_Link 2.png" style="width:14px; height:14px;" /></a></span>
                </h4>
                <div class="card-body" style="margin-left:5px; height:80px;">
                    <div class="card-header bold">Asigando A:</div>
                    <div class="card-header">@_t.AspNetUser.Email</div>
                    <div class="card-header"><sapm class="bold">Fecha Entrega:</sapm> @_t.FechaEntrega</div>
                </div>
            </div>
            <br />
            <br />
        }
    }
    else
    {
        foreach (var _t in Model.PTareas.Where(w => w.Estado_Id == _e.Id && w.AspNetUser.Email == User.Identity.Name).OrderBy(o => o.FechaEntrega))
        {
            <div class="card" style="border: 1px solid #dcdbdb; background-color: @_e.Color; border-radius: 5px;  height:120px;font-size:9pt; ">
                <h4 class="bold" style="padding-left:5px; border-bottom:1px solid #c7c4c4; background-color:#e8e5e5; margin-top:0px;">
                    @_t.Nombre
                    <span><a class="btn-grid" href="@Url.Action("EditModal", "PTareas", new { id = _t.Id }) "><img src="~/Content/image/iconos_Buscar.png" style="width:14px; height:14px;" /></a></span>
                    <span><a class="btn-grid" href="@Url.Action("CreateParteHoras", "PPartehoras", new { idt = _t.Id }) "><img src="~/Content/image/iconos_Link 2.png" style="width:14px; height:14px;" /></a></span>
                </h4>
                <div class="card-body" style="margin-left:5px; height:80px;">
                    <div class="card-header bold">Asigando A:</div>
                    <div class="card-header">@_t.AspNetUser.Email</div>
                    <div class="card-header"><sapm class="bold">Fecha Entrega:</sapm> @_t.FechaEntrega</div>
                </div>
            </div>
            <br />
            <br />
        }
    }
</div>
            }
        </div>
    </div>

</div>
